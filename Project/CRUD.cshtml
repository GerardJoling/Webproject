@using System
@using System.IO;
@{
    string contents = File.ReadAllText(@"c:\foo.txt");
    string label1 = "";
}
@{ 

    if(IsPost)
    {
        string path = Server.MapPath("~/Products/");
        string text = Request.Form["pass"];
        if (File.Exists(path + "product.cshtml"))
            {
            File.Create(path + "product" + 1 + ".cshtml");
            File.WriteAllText(path + "product.cshtml", "Hello");
        }
    }

}
<html>
<head>
    <title></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale-1">
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a href="#" class="navbar-brand">Cyberdine</a>
            </div>
            <div>
                <ul class="nav navbar-nav">
                    <li class="active"><a href="#">CRUD</a></li>
                    <li><a href="~/Forum.cshtml">Forum</a></li>
                </ul>
            </div>
        </div>
    </nav>﻿
    <form class="well form-horizontal" id="login_form" method="post">
        <fieldset>
            <input name="pass" placeholder="Password" class="form-control" required="required" type="password">
            <div class="form-group">
                <label class="col-md-4 control-label"></label>
                <div class="col-md-4">
                    <button name="addproduct" type="submit">Add Product</button>
                </div>
            </div>
        </fieldset>
    </form>
    @label1;
</body>
</html>